# React App Example

This example separates API access, hook-driven state transitions, and presentational components. It makes loading, error, and empty states explicit instead of hiding them behind a generic data helper.
